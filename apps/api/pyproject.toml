[project]
name = "vajratrace-api"
version = "0.1.0"
description = "VajraTrace FastAPI backend"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
